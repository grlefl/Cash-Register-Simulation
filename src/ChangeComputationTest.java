/**
 * This class serves as the entry point for running the ChangeComputation program.
 * When executed, it creates an instance of the ChangeComputation class, which simulates currency conversion,
 * payment processing, and change calculation.
 */
public class ChangeComputationTest {

    /**
     * Entry point for ChangeComputation program.
     * @param args The command-line arguments (not used in this program).
     */
    public static void main(String[] args) { new ChangeComputation();}
}
